Prepush copies all the scripts to folder specified in the .env file. Don't forget to add the folder to the PATH variable. Also, set up the git hooks directory:

```bash
git config core.hooksPath .githooks
```
